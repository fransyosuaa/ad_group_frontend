import RegisterLogin from '../form/registerLogin';
import Card from '../ui/card';

const LoginTemplate = () => {
  return (
    <div className="container">
      <Card>
        <RegisterLogin />
      </Card>
    </div>
  );
};

export default LoginTemplate;
