import UserLogs from '../form/userLogs';
import Card from '../ui/card';

const UserLogTemplate = () => {
  return (
    <div className="container">
      <Card>
        <UserLogs />
      </Card>
    </div>
  );
};

export default UserLogTemplate;
